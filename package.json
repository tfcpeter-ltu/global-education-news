{
  "name": "global-education-news",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-scholarship-seed.mjs && astro dev",
    "build": "node scripts/generate-scholarship-seed.mjs && astro build",
    "preview": "astro preview",
    "db:init": "node scripts/init-membership-db.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "astro": "^5.13.5",
    "pg": "^8.16.3"
  }
}

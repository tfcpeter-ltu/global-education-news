Girl: I would, but look at those clouds. Let us find a table inside.';v.q='What does the girl probably expect?';v.o=['It may rain','The restaurant will close','The food is expensive','They will leave'];v.a=0;v.why='提到 clouds 後改坐室內，可推論擔心下雨。'}v.variant=true;return v}
function renderNotesPage(){let box=document.getElementById('notesPage');if(!box)return;if(!profile){box.innerHTML='<div class="emptyState">請先建立學習檔案。</div>';return}if(profile.plan!=='paid'){box.innerHTML='<div class="card"><h3>完整會員功能</h3><p>Free 可使用每日 5 題與解析；長期弱點與筆記累積為完整會員功能。</p><button class="btn primary" onclick="openMemberInfo()">查看完整會員功能</button></div>';return}let n=profile.notes||{words:[],phr:[],gram:[],strategy:[]};box.innerHTML='<div class="notegrid"><div class="notecard"><h3>單字</h3><div class="pills">'+(n.words.length?n.words:['尚無']).map(x=>'<span class="pill">'+x+'</span>').join('')+'</div></div><div class="notecard"><h3>片語</h3><div class="pills">'+(n.phr.length?n.phr:['尚無']).map(x=>'<span class="pill">'+x+'</span>').join('')+'</div></div><div class="notecard"><h3>文法／句型</h3><div class="pills">'+(n.gram.length?n.gram:['尚無']).map(x=>'<span class="pill">'+x+'</span>').join('')+'</div></div><div class="notecard"><h3>閱讀／聽力策略</h3><ul>'+(n.strategy.length?n.strategy:['尚未產生']).map(x=>'<li>'+x+'</li>').join('')+'</ul></div></div>'}
function copyNotes(){if(!profile||profile.plan!=='paid'){openMemberInfo();return}let n=profile.notes,t='我的 AI 英文重點筆記\\n單字：'+(n.words.join('、')||'無')+'\\n片語：'+(n.phr.join('、')||'無')+'\\n文法：'+(n.gram.join('、')||'無')+'\\n策略：'+(n.strategy.join('；')||'無');navigator.clipboard?.writeText(t).then(()=>alert('已複製重點筆記。')).catch(()=>alert(t))}
function renderArchives(){archives.innerHTML='';for(let y=103;y<=115;y++)archives.innerHTML+='<div class="archive"><b>'+y+' 年</b><div style="font-size:10px;color:#66758b;margin-top:3px">閱讀｜聽力｜答案／說明</div><div class="resourceBtns"><button onclick="openArchive('+y+')">查看資源</button><button onclick="startArchivePractice('+y+')">AI 變化練習</button></div></div>'}
function openArchive(y){archiveTitle.textContent=y+' 年國中教育會考英文資源';archiveBody.innerHTML='<div class="detailgrid"><div class="detailcard"><h3>閱讀題本</h3><p>分析字彙、文法、克漏、主旨、細節、指涉與推論。</p></div><div class="detailcard"><h3>聽力題本／音檔</h3><p>建立辨識句意、基本問答與言談理解的 Listening DNA。</p></div><div class="detailcard"><h3>答案／說明</h3><p>核對正式答案與能力要求，再產生原創變化題。</p></div></div><button class="btn primary" onclick="startArchivePractice('+y+')">用這個年度的能力結構開始練習</button>';document.getElementById('archiveModal').classList.add('show')}
function startArchivePractice(y){if(!profile){openReg();return}daily=base.slice(0,profile.plan==='paid'?10:5).map(clone);di=0;dWrong=[];closeModal('archiveModal');renderDaily();dailyTitle.textContent='歷屆題型原創變化練習｜'+y+' 年能力結構';go('daily')}
function openPathway(){if(!profile){openReg();return}document.getElementById('pathModal').classList.add('show');pathResult.classList.add('hidden')}
function savePathway(){let dd=pDirection.value,e=pExperience.value,t=pTiming.value;profile.pathway={direction:dd,experience:e,timing:t};if(['overseas','planned'].includes(dd))profile.intent='high';saveP();let nm={live:'加拿大英文 Live Class',ossd:'OSSD 台加高中雙聯',exchange:'加拿大／英國交換',early:'會考後 Early Start'}[e];pathResult.innerHTML='<b>你的初步路線建議</b><p>'+(dd==='tw'?'目前以台灣升學為主，可以先把 '+nm+' 當作高中英文延伸。':dd==='undecided'?'先維持會考／高中英文能力，同時用 '+nm+' 了解國際課程。':'可以把 '+nm+' 與高中課程規劃一起評估。')+'</p>';pathResult.classList.remove('hidden')}
function openInterest(){if(!profile){openReg();return}document.getElementById('interestModal').classList.add('show');document.querySelectorAll('.iInterest').forEach(x=>x.checked=(profile.interests||[]).includes(x.value));interestSaved.classList.add('hidden')}
function saveInterest(){if(!iConsent.checked){alert('請先勾選同意。');return}profile.interests=[...document.querySelectorAll('.iInterest:checked')].map(x=>x.value);profile.interestTiming=iTiming.value;if(profile.interests.length&&profile.intent==='none')profile.intent='some';saveP();interestSaved.innerHTML='<b>已儲存</b><p>興趣已寫入目前學生檔案。</p>';interestSaved.classList.remove('hidden');renderAdmin()}
function renderAdmin(){let ss=JSON.parse(localStorage.getItem(KS)||'[]'),now=Date.now(),ina=x=>now-new Date(x.lastActivity||0)>30*864e5;let active=ss.filter(x=>x.status!=='suspended').length,inactive=ss.filter(ina).length,leads=ss.filter(x=>['some','high'].includes(x.intent)||x.interests?.length).length;aActive.textContent=active+' / 500';a30.textContent=ss.length-inactive;aInactive.textContent=inactive;aLeads.textContent=leads;let ff=document.getElementById('adminFilter')?.value||'all',rows=ss.filter(x=>ff==='all'||ff==='active'&&x.status!=='suspended'&&!ina(x)||ff==='inactive'&&ina(x)||ff==='suspended'&&x.status==='suspended'||ff==='lead'&&(['some','high'].includes(x.intent)||x.interests?.length));adminRows.innerHTML=rows.length?rows.map(x=>'<tr><td>'+(x.nick||x.name)+'</td><td>'+(x.school||'—')+'</td><td>'+(x.track||'—')+'</td><td>'+(x.plan==='paid'?'Complete':'Free')+'</td><td>'+(Object.entries(x.weak||{}).sort((a,b)=>b[1]-a[
(()=>{const d=window.STUDY_NAV_DATA||{};const root=document.getElementById('country-peter');if(!root)return;const country=root.dataset.country||document.body.dataset.country||'';const label=root.dataset.label||'';const tagMap={uk:'英國',us:'美國',canada:'加拿大',australia:'澳洲',europe:'歐洲',japan:'日本',singapore:'新加坡','hong-kong':'香港'};const rows=(d.articles||[]).filter(x=>x.kind==='Peter文章'&&(x.country===country||(x.country==='global'&&Array.isArray(x.tags)&&x.tags.includes(tagMap[country])))).sort((a,b)=>(b.sourceDate||'').localeCompare(a.sourceDate||''));if(!rows.length){root.innerHTML='<p>Peter 相關文章正在整理中。</p>';return;}const source=x=>{const u=x.url||'';if(u.includes('facebook.com'))return 'Facebook 原始貼文';if(u.includes('ltuedu.net'))return 'LTU Blog 對應文';return '原始貼文直連待補';};const cards=rows.map(x=>`<article class="guide-card"><span class="tag">${x.country==='global'?'跨國延伸':'Peter 的國際教育講座'}</span><h3>${(x.title||'').replace('Peter延伸｜','').replace('Peter歷史文章｜','')}</h3><p>${x.articleType||x.major||''}${x.sourceDate?` · ${x.sourceDate}`:''}</p><p><small>${x.status||''} · ${source(x)}</small></p><a href="${x.url}" target="_blank" rel="noopener">${(x.url||'').includes('ltuedu.net')&&!/\/l\//.test(x.url||'')?'前往文章目錄（直連待補）':'閱讀 Peter 原文'} →</a></article>`).join('');root.innerHTML=`<div class="section-heading"><div><span class="overline">Peter Article Library</span><h2>Peter 的國際教育講座｜${label||tagMap[country]||''}延伸閱讀</h2></div><p>除了主幹指南，也可以從 Peter 過去的文章看申請制度、科系比較、名校準備、職涯與實務觀點。每篇同時標示 Facebook 原始貼文、LTU Blog 對應文或直連待補狀態；涉及時效資訊的舊文也會標示更新狀態。</p></div><div class="guide-grid">${cards}</div><p style="margin-top:18px"><a class="dark-outline" href="../peter-library.html#peter-${country}">查看更多 Peter 文章 →</a></p>`;})();

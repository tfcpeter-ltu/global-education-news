(()=>{const data=window.UNIVERSITY_FINDER_DATA||[];const DS=window.STUDENT_DECISION_SUPPORT;const PI=window.PROGRAM_INSIGHTS;const OS=window.OFFICIAL_PROGRAM_SOURCES;const root=document.getElementById('compare-root');const clear=document.getElementById('clear-compare');const majorBox=document.getElementById('compare-major');let names=[];try{const v=JSON.parse(localStorage.getItem('studyNavigatorCompare')||'[]');names=Array.isArray(v)?[...new Set(v)].slice(0,4):[]}catch(e){}const major=localStorage.getItem('studyNavigatorCompareMajor')||'';const rows=names.map(n=>data.find(x=>x.name===n)).filter(Boolean);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));if(major&&majorBox){majorBox.style.display='block';majorBox.innerHTML=`<strong>目前比較科系：${esc(major)}</strong><br><span>以下比較盡量以同一個科系來判斷；若某校未列出該科系，請回選校器調整清單。</span>`;}if(!rows.length){root.innerHTML='<div class="empty"><h2>還沒有加入學校</h2><p>回到選校器，在學校卡片按「加入比較」。</p><p><a class="primary" href="universities.html">去找大學</a></p></div>';if(clear){clear.disabled=true;clear.textContent='沒有可清空的學校';}return;}const drows=rows.map(x=>({x,d:DS&&(!major||x.majors.includes(major))?DS.describe(x,major||x.majors?.[0]||'',{}):null,pi:PI&&(!major||x.majors.includes(major))?PI.get(x,major||x.majors?.[0]||''):null,src:OS?OS.get(x.name,major||x.majors?.[0]||''):null}));const tr=(label,fn)=>`<tr><th>${label}</th>${drows.map(o=>`<td>${fn(o)}</td>`).join('')}</tr>`;const options=[...new Set(rows.flatMap(x=>x.majors||[]))].sort();root.innerHTML=`<div class="docs-note"><label for="comparison-subject">比較科系</label><select id="comparison-subject"><option value="">請選擇共同科系</option>${options.map(m=>`<option value="${esc(m)}" ${major===m?'selected':''}>${esc(m)}</option>`).join('')}</select><p>${rows.length<2?'目前只有一間，請再加入至少一間大學。':'已選 '+rows.length+' 間；請比較相同學位層級及申請年度。'}</p><a href="universities.html?major=${encodeURIComponent(major)}">繼續加入同科系大學 →</a><p>全球研究聲望與當地就業須分開判讀；本站未取得可比的同年度雇主調查，不以 QS 推算就業名次。</p></div><div class="compare-wrap"><table class="compare-table"><thead><tr><th>比較項目</th>${drows.map(({x})=>`<th><div class="compare-school">${esc(x.name)}</div><small>${esc(x.countryLabel)} · ${esc(x.city)}</small><br><button type="button" class="remove-school" data-name="${esc(x.name)}">移除 ${esc(x.name)}</button></th>`).join('')}</tr></thead><tbody>${tr('科系符合情況',({x})=>!major?'請先在選校器選擇共同科系':x.majors.includes(major)?'資料庫列有此方向；仍需確認實際學位':'此校尚未列有選定科系，不能視為符合')} ${tr('官方查核狀態',({src,x})=>src?`<span class="v-ok">${esc(src.status)}</span><br><small>${esc(src.checked)} · ${esc(src.program)}</small><br><a href="${src.programUrl}" target="_blank" rel="noopener">官方課程頁 →</a>`:`<span class="v-pending">官方課程頁待逐校核對</span><br><a href="${x.url}" target="_blank" rel="noopener">大學官方網站 →</a>`)}${major?tr('這個科系的訓練定位',({pi})=>pi?esc(pi.focus):'依課程查核'):''}${major?tr('最適合哪種學生',({pi})=>pi?esc(pi.best):'依個人目標判讀'):''}${major?tr('當地專業／就業價值',({pi})=>pi?esc(pi.signal):'依城市與產業查核'):''}${major?tr('取捨提醒',({pi})=>pi?esc(pi.tradeoff):'需和其他學校一起比較'):''}${tr('實際課程與選校依據',({src})=>src?.evidence?esc(src.evidence.curriculum):'此校系尚無深化紀錄；下方通用提示不是課程官方要求。')}${tr('課程實習／研究安排',({src})=>src?.evidence?esc(src.evidence.experience):'待核對課程資料')}${tr('已查核入學要求',({src})=>src?.evidence?esc(src.evidence.requirements):'待核對當年度與申請學制')}${tr('已查核文件要求',({src})=>src?.evidence?esc(src.evidence.documents):'待核對逐校文件')}${tr('適合學生（本站判讀）',({src})=>src?.evidence?esc(src.evidence.interpretation):'尚無逐課程判讀')}${tr('限制與未確認事項',({src})=>src?.evidence?esc(src.evidence.limitation):'尚未取得足夠課程證據')}${tr('課程查核來源',({src})=>src?.evidence?src.evidence.sources.map((u,i)=>`<a href="${esc(u)}" target="_blank" rel="noopener">官方依據 ${i+1}</a>`).join('<br>'):'待補來源')}${tr('學校整體適合誰',({x})=>esc(x.note))}${tr('相關科系',({x})=>(x.majors||[]).join('、'))}${tr('全球聲望／研究',({d})=>d?`${esc(d.prestige)}；${esc(d.research)}`:'依科系查核')}${tr('當地就業／產業',({d})=>d?`${esc(d.local)}；${esc(d.employment)}`:'依城市與產業查核')}${tr('申請競爭度',({d})=>d?`${esc(d.competition)}<br><small>本站研究提示，非錄取率預測</small>`:'依科系判讀')}${tr('申請平台',({d,x})=>d?esc(d.docs.platform):esc(x.apply))}${tr('常見文件',({d})=>d?d.docs.docs.join('、'):'成績單、證明、英文等')}${tr('科系先修／重點',({d})=>d?esc(d.req.prereq):'逐校核對')}${tr('額外要求／選校提醒',({d})=>d?esc(d.req.extra):'逐校核對')}${tr('下一步',({x,src})=>`<a href="university-detail.html?school=${encodeURIComponent(x.name)}&major=${encodeURIComponent(major)}">完整分析</a><br><a href="apply.html?country=${encodeURIComponent(x.country)}&major=${encodeURIComponent(major)}">申請流程</a><br><a href="documents.html?country=${encodeURIComponent(x.country)}&major=${encodeURIComponent(major)}&school=${encodeURIComponent(x.name)}">文件清單</a><br><a href="${src?.programUrl||x.url}" target="_blank" rel="noopener">${src?'官方課程':'官方網站'}</a>`)}</tbody></table></div>`;document.getElementById('comparison-subject')?.addEventListener('change',e=>{localStorage.setItem('studyNavigatorCompareMajor',e.target.value);let meta={};try{meta=JSON.parse(localStorage.getItem('studyNavigatorShortMeta')||'{}')||{};}catch{}for(const school of rows){if(school.majors?.includes(e.target.value))meta[school.name]={...(meta[school.name]||{}),major:e.target.value};}localStorage.setItem('studyNavigatorShortMeta',JSON.stringify(meta));location.reload();});document.querySelectorAll('.remove-school').forEach(b=>b.addEventListener('click',()=>{localStorage.setItem('studyNavigatorCompare',JSON.stringify(names.filter(n=>n!==b.dataset.name)));location.reload();}));clear?.addEventListener('click',()=>{localStorage.removeItem('studyNavigatorCompare');localStorage.removeItem('studyNavigatorCompareMajor');location.reload();});})();

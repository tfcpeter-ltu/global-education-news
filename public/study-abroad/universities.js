(()=>{const data=window.UNIVERSITY_FINDER_DATA||[];const DS=window.STUDENT_DECISION_SUPPORT;const PI=window.PROGRAM_INSIGHTS;const OS=window.OFFICIAL_PROGRAM_SOURCES;const q=document.getElementById('uni-q');const country=document.getElementById('uni-country');const major=document.getElementById('uni-major');const goal=document.getElementById('uni-goal');const root=document.getElementById('uni-results');const count=document.getElementById('uni-count');const banner=document.getElementById('profile-banner');if(!root)return;const uniq=a=>[...new Set(a.filter(Boolean))].sort((a,b)=>a.localeCompare(b,'zh-Hant'));uniq(data.flatMap(x=>x.majors||[])).forEach(v=>major.insertAdjacentHTML('beforeend',`<option value="${v}">${window.STUDY_MAJOR_LABELS?.[v]?window.STUDY_MAJOR_LABELS[v]+" · ":""}${v}</option>`));uniq(data.flatMap(x=>x.goals||[])).forEach(v=>goal.insertAdjacentHTML('beforeend',`<option value="${v}">${v}</option>`));const p=new URLSearchParams(location.search);let saved={};try{saved=JSON.parse(localStorage.getItem('studyNavigatorProfile')||'{}')}catch(e){};if(!saved||typeof saved!=='object'||Array.isArray(saved))saved={};const pick=k=>p.has(k)?p.get(k):(saved[k]||'');const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));if(pick('country'))country.value=pick('country');if(pick('major')&&[...major.options].some(o=>o.value===pick('major')))major.value=pick('major');if(pick('goal')&&[...goal.options].some(o=>o.value===pick('goal')))goal.value=pick('goal');if(p.get('q'))q.value=p.get('q');const profile={curriculum:pick('curriculum'),grade:pick('grade'),english:pick('english'),budget:pick('budget'),priority:pick('priority'),need:pick('need')};if(banner&&Object.values(profile).some(Boolean)){banner.classList.add('show');banner.innerHTML=`<strong>你的初步條件：</strong> ${[profile.curriculum,profile.grade,profile.english,profile.budget,profile.priority].filter(Boolean).map(esc).join(' · ')}。${profile.priority?'結果會依你的優先目標調整研究順序；':'這些資料只用來提醒你核對條件；'}不會直接判定錄取機率。 <a href="student-start.html"><strong>修改條件 →</strong></a>`;}const text=x=>[x.name,x.nameZh,...(x.aliases||[]),...(x.majors||[]).map(m=>window.STUDY_MAJOR_LABELS?.[m]||m),x.city,x.countryLabel,...(x.majors||[]),...(x.goals||[]),x.note,x.apply].join(' ').toLowerCase();const selectedMajor=()=>major.value||'';const COMPARE='studyNavigatorCompare',SHORT='studyNavigatorShortlist',META='studyNavigatorShortMeta';const readList=k=>{try{const a=JSON.parse(localStorage.getItem(k)||'[]');return Array.isArray(a)?a.filter(v=>typeof v==='string'):[]}catch(e){return []}};const getCompare=()=>readList(COMPARE);const setCompare=a=>localStorage.setItem(COMPARE,JSON.stringify(a));const getShort=()=>readList(SHORT);const setShort=a=>localStorage.setItem(SHORT,JSON.stringify(a));const getMeta=()=>{try{const m=JSON.parse(localStorage.getItem(META)||'{}');return m&&typeof m==='object'&&!Array.isArray(m)?m:{}}catch(e){return {}}};const setMeta=m=>localStorage.setItem(META,JSON.stringify(m));const addCompare=name=>{let a=getCompare();if(!a.includes(name)){if(a.length>=4){alert('已選擇 4 間，請到比較表移除一間後再加入。');return false;}a.push(name);setCompare(a);}if(selectedMajor())localStorage.setItem('studyNavigatorCompareMajor',selectedMajor());renderToolBar();return true;};const addShort=name=>{let a=getShort();if(!a.includes(name)){a.push(name);setShort(a);}if(selectedMajor()){const m=getMeta();m[name]={...(m[name]||{}),major:selectedMajor()};setMeta(m);}renderToolBar();};const removeShort=name=>{setShort(getShort().filter(n=>n!==name));const m=getMeta();delete m[name];setMeta(m);renderToolBar();};const renderToolBar=()=>{let bar=document.getElementById('student-tool-bar');const s=getShort(),c=getCompare();if(!s.length&&!c.length){bar?.remove();return;}if(!bar){bar=document.createElement('div');bar.id='student-tool-bar';bar.style='position:fixed;left:16px;right:16px;bottom:74px;z-index:90;background:#10243d;color:white;border-radius:16px;padding:11px 14px;display:flex;gap:10px;align-items:center;justify-content:space-between;box-shadow:0 12px 35px rgba(0,0,0,.2);flex-wrap:wrap';document.body.appendChild(bar);}bar.innerHTML=`<span>我的選校 ${s.length} 間${c.length?` · 比較 ${c.length} 間`:''}</span><span style="display:flex;gap:7px;flex-wrap:wrap"><a href="shortlist.html" style="background:white;color:#10243d;padding:8px 11px;border-radius:9px;font-weight:800">我的選校 →</a>${c.length?'<a href="compare.html" style="border:1px solid rgba(255,255,255,.45);color:white;padding:8px 11px;border-radius:9px;font-weight:800">比較表 →</a>':''}</span>`;};const decisionKey=m=>m;const rankOf=x=>DS&&DS.rank?DS.rank(x,profile,goal.value):{score:0,reasons:[]};const goalMatch=x=>{if(!goal.value)return true;const gs=(x.goals||[]).join(' ');const v=goal.value;if(v==='名校／研究')return /名校|研究/.test(gs);if(v==='就業')return /就業|Co-op|實務|科技|產業|城市/.test(gs);if(v==='Co-op／就業')return /Co-op|就業|實務/.test(gs);if(v==='實務導向')return /實務|Co-op|就業|藝術|音樂|科技/.test(gs);if(v==='藝術設計')return /藝術|設計|作品/.test(gs);if(v==='音樂／表演')return /音樂|表演|作品|audition/i.test(gs);return (x.goals||[]).includes(v);};const card=x=>{const d=DS?DS.describe(x,selectedMajor(),profile):null;const pi=PI?PI.get(x,selectedMajor()):null;const source=OS?OS.get(x.name,selectedMajor()):null;const dk=decisionKey(selectedMajor());const savedShort=getShort().includes(x.name);const ri=rankOf(x);return `<article class="uni-card"><div class="uni-card-top"><span class="overline">${x.countryLabel}</span><span class="city">${x.city}</span></div><h3>${x.name}</h3>${x.nameZh?`<p class="school-name-zh">${esc(x.nameZh)}</p>`:""}<span class="verify-badge ${source?'verify-ok':'verify-pending'}">${source?`${source.status} · ${source.checked}`:'官方課程頁待逐校核對'}</span>${ri.reasons.length?`<div style="background:#eef4f8;border:1px solid #cbd9e6;border-radius:11px;padding:9px 11px;margin:8px 0;font-size:12px"><strong>為什麼優先顯示：</strong>${ri.reasons.join('；')}</div>`:''}${d?.warnings?.length?`<div style="background:#fff7e9;border:1px solid #ead2a6;border-radius:11px;padding:9px 11px;margin:8px 0;font-size:12px"><strong>你的條件提醒：</strong><br>${d.warnings.map(w=>`• ${w}`).join('<br>')}</div>`:''}<p class="fit"><strong>適合誰：</strong>${x.note}</p><div class="uni-tags">${(x.majors||[]).map(m=>`<span>${m}</span>`).join('')}</div>${pi&&selectedMajor()?`<div class="program-box"><h4>${selectedMajor()}｜為什麼把這間放進清單？</h4><p><b>課程／訓練定位：</b>${pi.focus}<br><small>${pi.scope||''}</small></p><p><b>最適合：</b>${pi.best}</p><p><b>當地專業／就業價值：</b>${pi.signal}</p><p><b>取捨提醒：</b>${pi.tradeoff}</p>${source?`<p><b>已核對官方課程：</b>${source.program}</p>`:''}</div>`:''}${d?`<div class="decision-grid"><div class="decision-item"><b>全球聲望／研究</b><span>${d.prestige}；${d.research}</span></div><div class="decision-item"><b>當地就業／產業</b><span>${d.local}；${d.employment}</span></div><div class="decision-item"><b>申請競爭度</b><span>${d.competition}</span></div><div class="decision-item"><b>申請平台</b><span>${d.docs.platform}</span></div></div><div class="req-box"><h4>你要準備什麼？</h4><div class="doc-list">${d.docs.docs.map(v=>`<span>${v}</span>`).join('')}</div><p><strong>這個科系要特別確認：</strong>${d.req.prereq}</p><p><strong>選校提醒：</strong>${d.req.extra}</p><p><small>${d.docs.note}</small></p></div>`:`<p><strong>申請方向：</strong>${x.apply}</p>`}${OS?.renderEvidence(source)||''}<div class="uni-actions"><a class="primary-link" href="university-detail.html?school=${encodeURIComponent(x.name)}&major=${encodeURIComponent(selectedMajor())}">完整分析</a><button type="button" class="short-btn" data-school="${x.name.replace(/"/g,'&quot;')}">${savedShort?'★ 已收藏':'☆ 收藏'}</button>${source?`<a href="${source.programUrl}" target="_blank" rel="noopener">官方課程</a>`:`<a href="${x.url}" target="_blank" rel="noopener">官方網站</a>`}<a href="apply.html?country=${encodeURIComponent(x.country)}&major=${encodeURIComponent(selectedMajor())}">怎麼申請</a><a href="documents.html?country=${encodeURIComponent(x.country)}&major=${encodeURIComponent(selectedMajor())}&school=${encodeURIComponent(x.name)}">準備文件</a>${dk?`<a href="major-decision.html?major=${encodeURIComponent(dk)}">科系怎麼選</a>`:''}<a href="peter-library.html?country=${encodeURIComponent(x.country)}&q=${encodeURIComponent(selectedMajor()||x.name)}">Peter 相關文章</a><button type="button" class="compare-btn" data-school="${x.name.replace(/"/g,'&quot;')}">＋ 加入比較</button></div></article>`};const bindTools=()=>{document.querySelectorAll('.compare-btn').forEach(b=>b.addEventListener('click',()=>{if(addCompare(b.dataset.school)!==false)b.textContent='✓ 已加入比較';}));document.querySelectorAll('.short-btn').forEach(b=>b.addEventListener('click',()=>{const name=b.dataset.school;if(getShort().includes(name)){removeShort(name);b.textContent='☆ 收藏';}else{addShort(name);b.textContent='★ 已收藏';}}));};const similarMajors=m=>{const map={'AI':['Computer Science','Data Science'],'Data Science':['Computer Science','AI','Mathematics'],'Medicine':['Biomedical Sciences','Pharmacy'],'Dentistry':['Medicine','Biomedical Sciences'],'Art & Design':['Animation','Fashion','Architecture'],'Music Production':['Music Performance','Composition'],'Business':['Finance','Economics'],'Finance':['Business','Economics'],'Engineering':['Computer Science','Mathematics']};return map[m]||[]};const render=()=>{const term=(q.value||'').trim().toLowerCase();const rows=data.filter(x=>(!term||text(x).includes(term))&&(!country.value||x.country===country.value)&&(!major.value||(x.majors||[]).includes(major.value))&&goalMatch(x)).sort((a,b)=>rankOf(b).score-rankOf(a).score);const priorityText=profile.priority||goal.value;count.textContent=`找到 ${rows.length} 間資料庫中的大學。${priorityText?`目前依「${priorityText}」調整研究順序。`:''}先收藏真正願意深入研究的學校，再到「我的選校」追蹤申請進度。`;if(rows.length){root.innerHTML=rows.map(card).join('')}else{const sims=similarMajors(major.value);const sameCountry=[];root.innerHTML=`<div class="finder-empty"><h3>這組條件目前沒有直接結果</h3><p>這不代表沒有這個科系。先放寬目標，或改查相近科系${sims.length?`：${sims.join('、')}`:''}。</p><div class="fallback-actions">${goal.value?'<button type="button" id="clear-goal">先取消目標篩選</button>':''}${q.value?'<button type="button" id="clear-query">清除搜尋文字</button>':''}${country.value?'<button type="button" id="clear-country">保留科系，查看其他國家</button>':''}<button type="button" id="reset-filters">重設全部篩選</button>${sims.map(s=>`<a href="universities.html?country=${encodeURIComponent(country.value)}&goal=&major=${encodeURIComponent(s)}">查 ${s}</a>`).join('')}<a href="majors.html">回科系探索</a></div>${sameCountry.length?`<div class="fallback-grid">${sameCountry.map(card).join('')}</div>`:''}</div>`;document.getElementById('clear-goal')?.addEventListener('click',()=>{goal.value='';render()});document.getElementById('clear-query')?.addEventListener('click',()=>{q.value='';render()});document.getElementById('clear-country')?.addEventListener('click',()=>{country.value='';render()});document.getElementById('reset-filters')?.addEventListener('click',()=>{q.value='';country.value='';major.value='';goal.value='';render()})}bindTools();renderToolBar();const u=new URL(location.href);u.searchParams.set('country',country.value);u.searchParams.set('major',major.value);u.searchParams.set('goal',goal.value);term?u.searchParams.set('q',q.value):u.searchParams.delete('q');history.replaceState(null,'',u);};[q,country,major,goal].forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',render));render();})();

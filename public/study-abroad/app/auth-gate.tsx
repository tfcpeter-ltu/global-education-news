"use client";
import {FormEvent,useEffect,useState} from 'react';
import {getSession,signIn,signOut} from '@/lib/supabase-client';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
export default function AuthGate({children}:{children:React.ReactNode}){const [ready,setReady]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>setReady(!!getSession()?.access_token),[]);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await signIn(email,password);setReady(true)}catch(x:any){setError(x.message||'登入失敗')}finally{setBusy(false)}}if(!ready)return <main className="auth-page"><section className="auth-card"><div className="eyebrow">LTU EDUCATION · COUNSELLING</div><h1>LTU OSSD 升學輔導資料庫</h1><p>限授權輔導人員登入使用。</p><form onSubmit={submit}><label>Email<Input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>密碼<Input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<p className="notice">{error}</p>}<Button type="submit" disabled={busy}>{busy?'登入中…':'登入管理系統'}</Button></form></section></main>;return <><button className="auth-signout" onClick={signOut}>登出</button>{children}</>}

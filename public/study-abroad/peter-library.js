(()=>{const d=window.STUDY_NAV_DATA||{};const all=(d.articles||[]).filter(x=>x.kind==='Peter文章');const labels={uk:'英國',us:'美國',canada:'加拿大',australia:'澳洲',europe:'歐洲',japan:'日本',singapore:'新加坡','hong-kong':'香港',global:'跨國／多國比較'};const tagNames={uk:'英國',us:'美國',canada:'加拿大',australia:'澳洲',europe:'歐洲',japan:'日本',singapore:'新加坡','hong-kong':'香港'};const order=['canada','uk','us','australia','europe','japan','singapore','hong-kong','global'];const fbPage='https://www.facebook.com/profile.php?id=61585164670358';const root=document.getElementById('peter-library');const count=document.getElementById('peter-count');const resultCount=document.getElementById('peter-filter-count');const q=document.getElementById('peter-q');const countryF=document.getElementById('peter-country-filter');const typeF=document.getElementById('peter-type-filter');const stageF=document.getElementById('peter-stage-filter');const compareF=document.getElementById('peter-compare-filter');const sourceF=document.getElementById('peter-source-filter');if(count)count.textContent=`目前已整理 ${all.length} 篇，持續從 Peter 的國際教育講座歷史文章庫批次匯入。`;if(!root)return;const sourceMeta=x=>{const u=x.url||'';if(u.includes('facebook.com'))return {key:'facebook',platform:'Facebook 原始貼文',link:'原始貼文直連已確認'};if(u.includes('ltuedu.net'))return {key:'blog',platform:/\/l\//.test(u)?'LTU Blog 文章':'LTU Blog 目錄',link:/\/l\//.test(u)?(x.linkChecked?'原文連結核對：'+x.linkChecked:'原文連結待重新核對'):'個別文章直連待補'};return {key:'pending',platform:'Peter 的國際教育講座',link:'原始貼文直連待補'};};const compareMode=x=>{const t=[x.articleType||'',...(x.tags||[])].join(' ');if(/橫向|跨國|跨校|比較/.test(t)&&!/路徑|階段|進程/.test(t))return 'horizontal';if(/縱向|路徑|階段|銜接|Foundation|Diploma|Pathway|學制/.test(t))return 'vertical';return 'single';};const uniq=(arr)=>[...new Set(arr.filter(Boolean))].sort((a,b)=>a.localeCompare(b,'zh-Hant'));uniq(all.map(x=>x.articleType)).forEach(v=>typeF&&typeF.insertAdjacentHTML('beforeend',`<option value="${v}">${v}</option>`));uniq(all.map(x=>x.stage)).forEach(v=>stageF&&stageF.insertAdjacentHTML('beforeend',`<option value="${v}">${v}</option>`));const params=new URLSearchParams(location.search);if(q&&params.get('q'))q.value=params.get('q');if(countryF&&params.get('country'))countryF.value=params.get('country');if(typeF&&params.get('type'))typeF.value=params.get('type');if(stageF&&params.get('stage'))stageF.value=params.get('stage');if(compareF&&params.get('compare'))compareF.value=params.get('compare');if(sourceF&&params.get('source'))sourceF.value=params.get('source');const aliases={'computer science':'電腦|資訊|計算','ai':'人工智慧|人工智能','data science':'資料科學|數據','engineering':'工程','business':'商學|商科','finance':'金融|財務','accounting':'會計','psychology':'心理','medicine':'醫學|醫科','dentistry':'牙醫|牙科','pharmacy':'藥學','biomedical sciences':'生物醫學','art & design':'藝術|設計','animation':'動畫','film production':'電影|影視','architecture':'建築','fashion':'時尚','music performance':'音樂|表演','music production':'音樂|製作','communication':'傳播|媒體'};const text=x=>[x.title,x.major,x.articleType,x.stage,x.status,...(x.tags||[])].filter(Boolean).join(' ').toLowerCase();const card=(x,c)=>{const cross=x.country==='global'&&c!=='global';const s=sourceMeta(x);const mode=compareMode(x);const modeLabel=mode==='horizontal'?'橫向比較':mode==='vertical'?'縱向比較':'單一主題／學校';return `<article class="peter-card"><div class="peter-meta"><span>${cross?'跨國延伸':(labels[x.country]||x.country)}</span><span>${x.sourceDate||''}</span><span>${x.articleType||''}</span><span>${x.stage||''}</span><span>${modeLabel}</span><span>${x.status||''}</span><span>${s.platform}</span><span>${s.link}</span></div><h3>${(x.title||'').replace('Peter延伸｜','').replace('Peter歷史文章｜','')}</h3><p>${x.major||''}</p>${Array.isArray(x.tags)?`<div class="peter-tags">${x.tags.map(t=>`<em>${t}</em>`).join('')}</div>`:''}<a href="${x.url}" target="_blank" rel="noopener">${(x.url||'').includes('ltuedu.net')&&!/\/l\//.test(x.url||'')?'前往文章目錄（直連待補）':'閱讀 Peter 原文'} →</a></article>`};const matches=x=>{const term=(q?.value||'').trim().toLowerCase();if(term&&!(text(x).includes(term)||(aliases[term]&&new RegExp(aliases[term]).test(text(x)))))return false;if(countryF?.value){const c=countryF.value;if(x.country!==c&&!(x.country==='global'&&c!=='global'&&Array.isArray(x.tags)&&x.tags.includes(tagNames[c])))return false;}if(typeF?.value&&x.articleType!==typeF.value)return false;if(stageF?.value&&x.stage!==stageF.value)return false;if(compareF?.value&&compareMode(x)!==compareF.value)return false;if(sourceF?.value&&sourceMeta(x).key!==sourceF.value)return false;return true;};const syncUrl=()=>{const p=new URLSearchParams();if(q?.value.trim())p.set('q',q.value.trim());if(countryF?.value)p.set('country',countryF.value);if(typeF?.value)p.set('type',typeF.value);if(stageF?.value)p.set('stage',stageF.value);if(compareF?.value)p.set('compare',compareF.value);if(sourceF?.value)p.set('source',sourceF.value);const next=`${location.pathname}${p.toString()?`?${p}`:''}${location.hash||''}`;history.replaceState(null,'',next);};const render=()=>{root.innerHTML='';const filtered=all.filter(matches);if(resultCount)resultCount.textContent=`符合條件 ${filtered.length} 篇／已整理 ${all.length} 篇`;if(!filtered.length){root.innerHTML='<div class="peter-empty">目前沒有符合條件的文章，請放寬一個篩選條件。</div>';syncUrl();return;}const rowsFor=c=>c==='global'?filtered.filter(x=>x.country==='global'):filtered.filter(x=>x.country===c||(x.country==='global'&&Array.isArray(x.tags)&&x.tags.includes(tagNames[c])));order.forEach(c=>{const rows=rowsFor(c).sort((a,b)=>(b.sourceDate||'').localeCompare(a.sourceDate||''));if(!rows.length)return;root.insertAdjacentHTML('beforeend',`<section class="peter-country" id="peter-${c}"><div class="peter-country-head"><span>Peter Article Library</span><h2>${labels[c]}</h2><p>${c==='global'?'跨國科系比較、職涯分析與 Peter 觀點。':'此區同時收錄該國專文，以及與該國相關的跨國比較文章。'}</p></div><div class="peter-grid">${rows.map(x=>card(x,c)).join('')}</div></section>`);});root.insertAdjacentHTML('beforeend',`<section class="peter-country"><div class="peter-note"><strong>Facebook 原始來源</strong><p>Peter 的國際教育講座 Facebook：已確認的原始貼文會優先提供直連；尚未取得直連的文章，先保留 LTU Blog 對應文或標示待補。</p><a href="${fbPage}" target="_blank" rel="noopener">前往 Peter 的國際教育講座 Facebook →</a></div></section>`);syncUrl();};[q,countryF,typeF,stageF,compareF,sourceF].filter(Boolean).forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',render));render();})();

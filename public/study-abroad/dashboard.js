(()=>{const data=window.UNIVERSITY_FINDER_DATA||[];const root=document.getElementById('dash-root'),title=document.getElementById('dash-title'),subtitle=document.getElementById('dash-subtitle');const PKEY='studyNavigatorProfile',SKEY='studyNavigatorShortlist',MKEY='studyNavigatorShortMeta';const read=(k,fallback)=>{try{const v=JSON.parse(localStorage.getItem(k)||JSON.stringify(fallback));return Array.isArray(fallback)?(Array.isArray(v)?v:[]):(v&&typeof v==='object'&&!Array.isArray(v)?v:fallback)}catch(e){return fallback}};const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const profile=read(PKEY,{}),names=read(SKEY,[]),meta=read(MKEY,{});const labels={uk:'英國',canada:'加拿大',us:'美國',australia:'澳洲',europe:'歐洲',japan:'日本',singapore:'新加坡','hong-kong':'香港'};const schools=names.map(n=>data.find(x=>x.name===n)).filter(Boolean);const statusOf=n=>(meta[n]||{}).status||'研究中';const deadlineOf=n=>(meta[n]||{}).deadline||'';const daysUntil=d=>{if(!d)return null;const t=new Date();t.setHours(0,0,0,0);return Math.ceil((new Date(d+'T00:00:00')-t)/86400000)};const submitted=schools.filter(x=>['已送出','等待結果','Offer'].includes(statusOf(x.name))).length;const offers=schools.filter(x=>statusOf(x.name)==='Offer').length;const upcoming=schools.filter(x=>{const d=daysUntil(deadlineOf(x.name));return d!==null&&d>=0&&d<=30}).length;const docKey=profile.country&&profile.major?`studyNavigatorDocs:${profile.country}:${profile.major}`:'';const docState=docKey?read(docKey,{}):{};const docsDone=Object.values(docState).filter(Boolean).length;const timelineKeys=[];for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i);if(k&&k.startsWith(`studyNavigatorTimeline:${profile.country||''}:${profile.major||''}:`))timelineKeys.push(k)};let timelineDone=0,timelineTotal=0,timelineIntake='';if(timelineKeys.length){timelineKeys.sort().reverse();timelineIntake=timelineKeys[0].split(':').pop();const s=read(timelineKeys[0],{});timelineDone=Object.entries(s).filter(([k,v])=>!/^\d+$/.test(k)&&v===true).length;timelineTotal=0}title.textContent=`${profile.major||'尚未設定科系'} · ${labels[profile.country]||'國家未定'}`;subtitle.textContent=[profile.curriculum,profile.grade,profile.english,profile.priority].filter(Boolean).join(' · ')||'先建立個人規劃，Dashboard 才能給你更有用的下一步。';const gaps=[];if(!profile.country)gaps.push('先決定至少一個主要國家，才能進一步判斷申請制度。');if(!profile.major)gaps.push('先選主幹科系，才能判斷 prerequisite、文件與學校。');if(!profile.english||/5.5|尚未/.test(profile.english))gaps.push('英文目前是優先核對項目：確認目標校門檻與是否需要 Pathway。');if(/Medicine|Dentistry/.test(profile.major||''))gaps.push('醫牙要提早確認 UCAT／其他測驗、面試與國際生名額。');if(/Art & Design|Animation|Film|Architecture|Fashion/.test(profile.major||''))gaps.push('作品集需要提早做，不要等到申請季才開始。');if(profile.major==='Music Production')gaps.push('先準備音訊製作作品集，逐課程確認面試與作品格式。');else if(/Music/.test(profile.major||''))gaps.push('Audition／作品與主修師資是核心，請提早準備。');if(!schools.length)gaps.push('目前還沒有收藏學校：先建立 5–10 間第一輪選校清單。');const metrics=`<div class="metrics"><div class="metric"><b>${schools.length}</b><span>收藏學校</span></div><div class="metric"><b>${submitted}</b><span>已進申請流程</span></div><div class="metric"><b>${offers}</b><span>Offer</span></div><div class="metric"><b>${upcoming}</b><span>30 天內 Deadline</span></div><div class="metric"><b>${docsDone}</b><span>文件已勾選</span></div><div class="metric"><b>${timelineDone}</b><span>時間表已完成</span></div></div>`;const chips=[profile.goal,profile.curriculum,profile.grade,profile.english,profile.budget,profile.priority].filter(Boolean).map(v=>`<span>${esc(v)}</span>`).join('');const next=gaps.length?gaps.map((g,i)=>`<div class="next-item"><strong>${i+1}. ${g}</strong></div>`).join(''):'<div class="next-item"><strong>目前沒有明顯缺口。下一步是逐校核對官方要求並維持申請進度。</strong></div>';const sorted=[...schools].sort((a,b)=>(deadlineOf(a.name)||'9999-12-31').localeCompare(deadlineOf(b.name)||'9999-12-31'));const schoolRows=sorted.length?sorted.map(x=>{const mm=meta[x.name]||{};const d=daysUntil(mm.deadline);return `<div class="school-row"><div><b>${x.name}</b><br><small>${x.countryLabel} · ${x.city}</small></div><div>${esc(mm.tier||'未分層')}</div><div>${esc(statusOf(x.name))}</div><div>${esc(mm.deadline||'未填 Deadline')}${d!==null&&d>=0&&d<=30?`<br><small>${d} 天後</small>`:''}</div><div>${mm.next?`下一步：${esc(mm.next)}`:`<a href="shortlist.html">補上下一步 →</a>`}</div></div>`}).join(''):'<div class="empty-dash"><h3>還沒有學校</h3><p><a href="universities.html"><strong>先去找大學 →</strong></a></p></div>';root.innerHTML=`${metrics}<div class="dash-grid"><section class="dash-card"><h2>我的條件</h2><div class="chips">${chips||'<span>尚未建立個人條件</span>'}</div><p style="margin-top:14px"><a href="student-start.html"><strong>修改條件 →</strong></a></p></section><section class="dash-card"><h2>現在最該做的事</h2><div class="next-list">${next}</div></section><section class="dash-card"><h2>文件與資格</h2><p>文件中心目前已勾選 <strong>${docsDone}</strong> 項。</p><p><a href="eligibility.html"><strong>資格核對 →</strong></a> · <a href="documents.html?country=${encodeURIComponent(profile.country||'')}&major=${encodeURIComponent(profile.major||'')}"><strong>文件中心 →</strong></a></p></section><section class="dash-card"><h2>時間表</h2><p>目前已完成 <strong>${timelineDone}</strong>${timelineTotal?`／${timelineTotal}`:''} 個時間表步驟${timelineKeys.length?`（${esc(timelineKeys[0].split(':').pop())} 入學年度）`:""}。</p><p><a href="timeline.html?country=${encodeURIComponent(profile.country||'')}&major=${encodeURIComponent(profile.major||'')}&intake=${encodeURIComponent(timelineIntake||profile.intake||2027)}&english=${encodeURIComponent(profile.english||'')}"><strong>繼續時間表 →</strong></a></p></section></div><section class="dash-card" style="margin-top:14px"><h2>我的申請清單</h2><div class="school-list">${schoolRows}</div></section><p class="print-only">本頁為個人規劃工作表；正式申請要求、Deadline、學費與簽證須以官方資訊為準。</p>`;document.getElementById('dash-print').addEventListener('click',()=>window.print());})();
